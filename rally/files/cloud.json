{%- set auth = salt['pillar.get']('rally:benchmark:provider:'+provider_name+':auth') -%}
{
    "type": "ExistingCloud",
    "endpoint": {
        "auth_url": "{{ cloud.auth_url }}",
        "username": "{{ cloud.username }}",
        "password": "{{ cloud.password }}",
        "tenant_name": "{{ cloud.tenant_name }}"
    }
}
